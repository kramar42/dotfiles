# Configs
config defaultToCurrentScreen true
config nudgePercentOf screenSize
config resizePercentOf screenSize
config secondsBetweenRepeat 0.1
config checkDefaultsOnLoad true
config focusCheckWidthMax 3000
config keyboardLayout dvorak
config windowHintsShowIcons true
config windowHintsIgnoreHiddenWindows false
#config windowHintsDuration 5
config windowHintsSpread true
#config windowHintsOrder persist

# Misc Aliases
alias showHintsLeftHand hint AOEUIYQJKX
alias showNormalHint hint AOEUIDHTNSYXFBPKGMCW

# Abstract positions
alias full          move screenOriginX;screenOriginY                  screenSizeX;screenSizeY
alias lefthalf      move screenOriginX;screenOriginY                  screenSizeX/2;screenSizeY
alias righthalf     move screenOriginX+screenSizeX/2;screenOriginY    screenSizeX/2;screenSizeY
alias top           move screenOriginX;screenOriginY                  screenSizeX;screenSizeY/2
alias bottom        move screenOriginX;screenOriginY+screenSizeY/2    screenSizeX;screenSizeY/2
alias rightmost     move screenOriginX+screenSizeX*0.4;screenOriginY  screenSizeX*0.6;screenSizeY
alias rightless     move screenOriginX+screenSizeX*0.6;screenOriginY  screenSizeX*0.4;screenSizeY
alias leftmost      move screenOriginX;screenOriginY                  screenSizeX*0.6;screenSizeY
alias leftless      move screenOriginX;screenOriginY                  screenSizeX*0.4;screenSizeY

# ======================================
# The HYPER key
# ======================================

# kinesis
#alias hyper ctrl;shift
alias hyper ctrl;cmd

bind x:${hyper} grid padding:5 1:6,2 0:8,3
bind y:${hyper} relaunch
# Location bindings
bind o:${hyper} activate-snapshot default
bind a:${hyper} snapshot default save-to-disk;stack
#bind tab:cmd switch
bind e:${hyper} ${lefthalf}
bind i:${hyper} ${full}
bind u:${hyper} ${righthalf}
bind j:${hyper} ${bottom}
bind k:${hyper} ${top}
bind -:${hyper} throw next
#bind p:${hyper} ${bottom}
#bind .:${hyper} ${top}

# Resize Bindings
#bind r:${hyper} ${leftmost}
#bind g:${hyper} ${rightmost}

# Focus Bindings
#bind right:${hyper}    focus right
#bind left:${hyper}     focus left
#bind up:${hyper}       focus up
#bind down:${hyper}     focus down

# App bindings
bind g:${hyper} focus 'Google Chrome'
bind c:${hyper} focus 'Firefox'
bind t:${hyper} focus 'iTerm2'
bind d:${hyper} focus 'Sourcetree'
bind f:${hyper} focus 'Spotify'
bind n:${hyper} focus 'Sublime Text'
bind m:${hyper} focus 'Mail'
bind h:${hyper} focus 'IntelliJ IDEA'
bind s:${hyper} focus 'Skype'
bind l:${hyper} focus 'Telegram'
#bind n:${hyper} focus 'Microsoft OneNote'
#bind d:${hyper} focus 'Todoist'
bind v:${hyper} focus 'Finder'
